apiVersion: v1
kind: Service
metadata:
  name: {{ include "basic-python-helm-chart.fullname" . }}
  labels:
    {{- include "basic-python-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "basic-python-helm-chart.selectorLabels" . | nindent 4 }}
